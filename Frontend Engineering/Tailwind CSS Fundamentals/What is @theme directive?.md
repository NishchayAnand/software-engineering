
The `@theme` directive is a Tailwind CSS v4+ feature that lets you define your design tokens (colors, spacing, fonts, radii, etc.) directly in CSS, and Tailwind automatically turns those tokens into utility classes.

In short:

> @theme = your design system, written in CSS, consumed by Tailwind.

---
