
In TypeScript, `Record` is a **utility type** that helps you define an **object type with specific keys and value types**.

```
type HotelRooms = Record<string, number>;
```

**This means:**

> An object whose keys are strings and values are numbers.

---

